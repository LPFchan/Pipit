#!/usr/bin/env bash
# Minimal gradle wrapper shim: delegates to `gradle` installed on PATH.
if command -v gradle >/dev/null 2>&1; then
  exec gradle "$@"
else
  echo "Gradle not found on PATH. Install Gradle or run 'gradle wrapper' in the project root to generate a proper wrapper." >&2
  exit 1
fi
